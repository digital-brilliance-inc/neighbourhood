export default async function Page() {
  // const providers = await fetch('/auth/providers');
  // console.log('providers = %o', providers);
  return (
    <div>
      <div>This is the list of signin options</div>
    </div>
  );
}
